import Guidelines from "@/Components/Guidelines/Guidelines";

export const metadata = {
  title: "Gameboxd - Guidelines",
  description: "Gameboxd community guidelines",
};

export default function GuidelinesPage() {
  return <Guidelines />;
}
